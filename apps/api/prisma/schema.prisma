// LongevIQ Prisma schema
// Datasource and generators. Models are added incrementally per Sprint.
// Sprint 1: User, RefreshToken, etc.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
